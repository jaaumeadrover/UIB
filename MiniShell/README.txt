Práctica realizada por:
- Jaume Adrover Fernandez
- Marc Cañellas Gomez
- Joan Balaguer Llagostera

nivelA: 
- No hemos añadido ningun elemento o función adicional
- No hay problemas de ejecución

nivelB: 
- No hemos añadido ningun elemento o función adicional
- Si ejecutamos el programa en una terminal de mac, al hacer "ctr+c" no vuelve a 
  salir el prompt hasta que puslses enter. En Windows i Linux, funciona correctamente

nivelC: 
- No hemos añadido ningun elemento o función adicional
- Si ejecutamos el programa en una terminal de mac, al hacer "ctr+c" no vuelve a 
  salir el prompt hasta que puslses enter. En Windows i Linux, funciona correctamente
- Cuando ejecutamos un proceso en segundo plano con & (por ejemplo "sleep 20 &"),
  y dejamos acabar la ejecución de este comando, el mensaje que muestra el método 
  execute_line() para mostrar la informacion del proceso hijo, se muestra justo 
  después de mostrar el prompt.
  
nivelD: 
- No hemos añadido ningun elemento o función adicional
- Si ejecutamos el programa en una terminal de mac, al hacer "ctr+c" no vuelve a 
  salir el prompt hasta que puslses enter. En Windows i Linux, funciona correctamente
- Cuando ejecutamos un proceso en segundo plano con & (por ejemplo "sleep 20 &"),
  y dejamos acabar la ejecución de este comando, el mensaje que muestra el método 
  execute_line() para mostrar la informacion del proceso hijo, se muestra justo 
  después de mostrar el prompt.

my_shell: 
- No hemos añadido ningun elemento o función adicional
- Si ejecutamos el programa en una terminal de mac, al hacer "ctr+c" no vuelve a 
  salir el prompt hasta que puslses enter. En Windows i Linux, funciona correctamente

